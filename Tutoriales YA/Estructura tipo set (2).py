# los conjuntos dados son:
A = {"z", 8, "9", (10, 20, 30), 8, 9, 8}
B = {7, 8, 9}

# Cuantos elemmentos tiene el conjunto A?
elementos = len(A)

# Como agregar el cojunto B los numeros 1, 2, 3 en una unica operacion?
B.add((1, 2, 3))
print(B)

# Es A menor que B o B menor que A?
# Ninguno de los dos son subconjuntos del otro